# Lane context — the API index for ONE page build

Generated from the files it describes; read it ONCE instead of grepping
`src/lib/journey`, `src/components/blocks`, `src/types/app.ts` or `src/services`.
Signatures are verbatim (bodies stripped); a props interface is the block's full contract.
Import everything under **@/lib/journey** from `@/lib/journey` (one barrel); blocks from
`@/components/blocks/<Name>`; the internal door `servicePort` from `@/services/journeyPort`,
the public door `publicPort` from `@/lib/journey/publicPort` (a public page only).

## Entities — fields as the platform has them (key · kind · label · flags)

### `kunden` — Kunden (type `Kunden`, one record: `KundenEntry`)
Display fields: `kundenname`
- `kundenname` · text · Name / Firmenname · required! · maxLength 4000
- `kundentyp` · lookup · Kundentyp · required! · options: firma | behoerde | sonstiges | einzelperson
- `email` · email · E-Mail · required!
- `anlagedatum` · date · Anlagedatum · required!
- `strasse` · text · Straße · required! · maxLength 4000
- `hausnummer` · text · Hausnummer · required! · maxLength 4000
- `plz` · text · Postleitzahl · required! · maxLength 4000
- `ort` · text · Ort · required! · maxLength 4000
- `rechnungsadresse_gleich` · bool · Rechnungsadresse ist identisch mit der Adresse
- `rechnungsstrasse` · text · Rechnungsstraße · maxLength 4000
- `rechnungshausnummer` · text · Rechnungs-Hausnummer · maxLength 4000
- `rechnungsplz` · text · Rechnungs-Postleitzahl · maxLength 4000
- `rechnungsort` · text · Rechnungsort · maxLength 4000
- `ansprechpartner_titel` · text · Titel des Ansprechpartners · maxLength 4000
- `ansprechpartner_vorname` · text · Vorname des Ansprechpartners · maxLength 4000
- `ansprechpartner_nachname` · text · Nachname des Ansprechpartners · maxLength 4000
- `ansprechpartner_email` · email · E-Mail des Ansprechpartners
- `bevorzugte_kontaktart` · lookup · Bevorzugte Kontaktart · options: email | telefon | post | persoenlich
- `letzter_kontakt_datum` · date · Datum des letzten Kontakts
- `letzter_kontakt_ansprechpartner` · text · Ansprechpartner beim letzten Kontakt · maxLength 4000
- `notizen` · textarea · Notizen
Input shapes: choice `kundentyp` (ChoiceGroup); choice `bevorzugte_kontaktart` (ChoiceGroup)

### `berater` — Berater (type `Berater`, one record: `BeraterEntry`)
Display fields: `vorname`, `nachname`
- `vorname` · text · Vorname · required! · maxLength 4000
- `nachname` · text · Nachname · required! · maxLength 4000
- `titel` · text · Titel (optional) · maxLength 4000
- `strasse` · text · Straße · maxLength 4000
- `hausnummer` · text · Hausnummer · maxLength 4000
- `plz` · text · Postleitzahl · maxLength 4000
- `ort` · text · Ort · maxLength 4000
- `email_beruflich` · email · E-Mail (beruflich) · required!
- `email_privat` · email · E-Mail (privat)
- `einstiegsdatum` · date · Einstiegsdatum
- `status` · lookup · Status · required! · options: aktiv | urlaub | elternzeit | sonstiges
- `stundensatz` · number · Stundensatz (€/h) · currency
- `stunden_aktueller_monat` · number · Gebuchte Stunden – aktueller Monat
- `stunden_aktuelles_quartal` · number · Gebuchte Stunden – aktuelles Quartal
- `stunden_aktuelles_jahr` · number · Gebuchte Stunden – aktuelles Jahr
- `stunden_letzter_monat` · number · Gebuchte Stunden – letzter Monat
- `stunden_letztes_quartal` · number · Gebuchte Stunden – letztes Quartal
- `stunden_letztes_jahr` · number · Gebuchte Stunden – letztes Jahr
- `sonstiges` · textarea · Sonstige Anmerkungen
- `leistungen` · multirecord · Zugeordnete Leistungen · → `leistungskatalog`
- `zugewiesene_projekte` · multirecord · Aktuell zugewiesene Projekte · → `projekte`
Input shapes: choice `status` (ChoiceGroup)

### `leistungskatalog` — Leistungskatalog (type `Leistungskatalog`, one record: `LeistungskatalogEntry`)
Display fields: `leistungsname`
- `leistungsname` · text · Leistungsname · required! · maxLength 4000
- `leistungstyp` · lookup · Leistungstyp · required! · options: beratung | entwicklung | schulung | support | konzeption | sonstiges
- `beschreibung` · textarea · Beschreibung
- `kostenvoranschlag` · number · Normaler Kostenvoranschlag (€) · currency
- `einheit` · lookup · Abrechnungseinheit · options: pro_stunde | pro_tag | pauschal | pro_monat
- `ausfuehrende_berater` · multirecord · Ausführende Berater · → `berater`
Input shapes: choice `leistungstyp` (ChoiceGroup); choice `einheit` (ChoiceGroup)

### `projekte` — Projekte (type `Projekte`, one record: `ProjekteEntry`)
Display fields: `projektkennung`
- `projektkennung` · text · Projektkennung · required! · maxLength 4000
- `projektnummer` · number · Projektnummer · required!
- `projektart` · lookup · Projektart · required! · options: it_beratung | entwicklung | schulung | konzeption | support | sonstiges
- `projektstatus` · lookup · Projektstatus · required! · options: in_bearbeitung | akquise | abgeschlossen
- `projektstart_monat` · lookup · Startmonat · options: januar | februar | maerz | april | mai | juni | juli | august | september | oktober | november | dezember
- `projektstart_jahr` · number · Startjahr
- `kunde` · record · Kunde · required! · → `kunden`
- `ansprechpartner_kunde` · text · Ansprechpartner beim Kunden · maxLength 4000
- `letzter_schritt` · textarea · Letzter Schritt / aktueller Stand
- `projektleitung` · record · Projektleitung · → `berater`
Input shapes: choice `projektart` (ChoiceGroup); choice `projektstatus` (ChoiceGroup)

### `angebote` — Angebote (type `Angebote`, one record: `AngeboteEntry`)
Display fields: `dauer`
- `angebotsnummer` · number · Angebotsnummer · required!
- `angebotsjahr` · number · Jahr · required!
- `angebotstyp` · lookup · Angebotstyp · required! · options: dienstleistungsangebot | wartungsvertrag | projektangebot | rahmenvertrag | sonstiges
- `angebotsstatus` · lookup · Angebotsstatus · options: entwurf | versendet | angenommen | abgelehnt
- `zeitrahmen_anfang` · date · Beginn · required!
- `zeitrahmen_ende` · date · Ende (optional)
- `dauer` · text · Dauer · maxLength 4000
- `kostentyp` · lookup · Kostentyp · options: einmalig | monatlich | jaehrlich | quartalsweise | sonstiges
- `kostenbetrag` · number · Kostenbetrag (€) · currency
- `beschreibung` · textarea · Beschreibung / Leistungsumfang
- `anhang` · file · Anhang (z. B. Angebots-Template) · read-only
- `projekt` · record · Zugewiesenes Projekt · → `projekte`
- `berater` · record · Zuständiger Berater · → `berater`
Input shapes: choice `angebotstyp` (ChoiceGroup); choice `angebotsstatus` (ChoiceGroup); choice `kostentyp` (ChoiceGroup)

### `zeiterfassung` — Zeiterfassung (type `Zeiterfassung`, one record: `ZeiterfassungEntry`)
Display fields: `taetigkeit`
- `berater` · record · Berater · required! · → `berater`
- `projekt` · record · Projekt · required! · → `projekte`
- `leistung` · record · Erbrachte Leistung · → `leistungskatalog`
- `datum` · date · Datum · required!
- `stunden` · number · Anzahl Stunden · required!
- `erfassungsmonat` · lookup · Abrechnungsmonat · options: februar | maerz | april | mai | juni | juli | august | september | oktober | november | dezember | januar
- `erfassungsjahr` · number · Abrechnungsjahr
- `abrechenbar` · bool · Abrechenbar
- `taetigkeit` · textarea · Tätigkeitsbeschreibung

### `rechnungen` — Rechnungen (type `Rechnungen`, one record: `RechnungenEntry`)
Display fields: `rechnungsnummer`
- `rechnungsnummer` · text · Rechnungsnummer · required! · maxLength 4000
- `rechnungsdatum` · date · Rechnungsdatum · required!
- `faelligkeitsdatum` · date · Fälligkeitsdatum
- `rechnungsstatus` · lookup · Rechnungsstatus · options: entwurf | versendet | bezahlt | ueberfaellig | storniert
- `rechnungsmonat` · lookup · Abrechnungsmonat · options: januar | februar | maerz | april | mai | juni | juli | august | september | oktober | november | dezember
- `rechnungsjahr` · number · Abrechnungsjahr
- `kunde` · record · Kunde · required! · → `kunden`
- `nettobetrag` · number · Nettobetrag (€) · currency
- `mehrwertsteuer` · number · Mehrwertsteuer (%)
- `gesamtbetrag` · number · Gesamtbetrag (€) · currency
- `notizen` · textarea · Notizen
- `anhang` · file · Rechnungsdokument (PDF) · read-only
- `projekt` · record · Projekt · required! · → `projekte`
- `berater` · multirecord · Beteiligte Berater · → `berater`
- `zeiterfassungseintraege` · multirecord · Zeiterfassungseinträge · → `zeiterfassung`
Input shapes: choice `rechnungsstatus` (ChoiceGroup)

## Flow hooks — generated from the plan, ONE per flow (`@/lib/journey/flows/<Pascal>`)

Each owns its flow's forms, picks and submit plan; the page composes. The docblock is the contract:

### `lib/journey/flows/AngebotErstellen`
```ts
/**
 * useAngebotErstellenFlow — the plumbing of the flow « Angebot erstellen », generated from the plan.
 *
 * Writes `angebote`: asks `projekt`, `berater`, `angebotstyp`, `zeitrahmen_anfang`, `zeitrahmen_ende`, `dauer`, `kostentyp`, `kostenbetrag`, `beschreibung`; sets `angebotsstatus` itself.
 * The hook OWNS: the form(s) with exactly these fields and the plan's required
 * ingredients, one record search per picked field (columns and filter from
 * the plan), and the submit plan with its fixed and derived values. A page
 * that only calls `flow.submit.run()` cannot write a field the plan does not
 * know — there is no way to spell it.
 *
 * YOU decide what a person notices, through the options:
 *   steps     which wizard step asks which field (default: one step per pick,
 *             then one for the typed fields, then "Prüfen" = step 4)
 *   items     how a search hit is displayed per pick (title, subtitle, status …)
 *   initial   prefills for typed fields
 *   messages  the sentence for an empty required field, per field
 *
 *   const flow = useAngebotErstellenFlow({
 *     steps: { projekt: 1, berater: 2, angebotstyp: 3, zeitrahmen_anfang: 3, zeitrahmen_ende: 3, dauer: 3, kostentyp: 3, kostenbetrag: 3, beschreibung: 3 },
 *     items: { projekt: r => ({ id: r.id, title: fieldText(r, 'projektkennung') }) },
 *   });
 *   <IntentWizardShell forms={flow.forms} draftKey={flow.draftKey} …>
 *     <EntitySelectStep {...flow.picks.projekt.select} {...flow.pick('projekt')} />
 *     <EntitySelectStep {...flow.picks.berater.select} {...flow.pick('berater')} />
 *     <Bound form={flow.forms.angebote} name="angebotstyp" />
 *     <Bound form={flow.forms.angebote} name="zeitrahmen_anfang" />
 *     <Bound form={flow.forms.angebote} name="zeitrahmen_ende" />
 *     <Bound form={flow.forms.angebote} name="dauer" />
 *     <Bound form={flow.forms.angebote} name="kostentyp" />
 *     <Bound form={flow.forms.angebote} name="kostenbetrag" />
 *     <Bound form={flow.forms.angebote} name="beschreibung" />
 *     <StepNav onNext={() => flow.validateStep(n)} />
 *     {!flow.submit.done && <SummaryStep forms={flow.formList} submit={flow.submit} />}
 *     {flow.submit.result && <SuccessStep result={flow.submit.result} forms={flow.formList} submit={flow.submit} />}
 *   </IntentWizardShell>
 */
```
- `export type AngebotErstellenFieldKey = 'angebotstyp' | 'berater' | 'beschreibung' | 'dauer' | 'kostenbetrag' | 'kostentyp' | 'projekt' | 'zeitrahmen_anfang' | 'zeitrahmen_ende';`
```ts
export interface AngebotErstellenForms {
  angebote: StepForm<'angebote'>;
}
```
```ts
export interface AngebotErstellenFlowOptions {
  /** field → wizard step that asks it; drives „Ändern“ links and answer chips. */
  steps?: Partial<Record<Key, number>>;
  initial?: Partial<Record<Key, unknown>>;
  messages?: Partial<Record<Key, string>>;
  /** How a search hit reads — the card's title/subtitle/status per pick. */
  items?: {
    projekt?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
    berater?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
  };
}
```
- `export const ANGEBOTERSTELLEN_REVIEW_STEP = 4;`
- `export function useAngebotErstellenFlow(options: AngebotErstellenFlowOptions = {})`
- `export type AngebotErstellenFlow = ReturnType<typeof useAngebotErstellenFlow>;`

### `lib/journey/flows/AngebotsstatusAendern`
```ts
/**
 * useAngebotsstatusAendernFlow — the plumbing of the flow « Angebotsstatus aktualisieren », generated from the plan.
 *
 * Changes `angebote`: the record to change is picked (`flow.pick('angebote')`), the form is prefilled with its values; asks `angebotsstatus`.
 * The hook OWNS: the form(s) with exactly these fields and the plan's required
 * ingredients, one record search per picked field (columns and filter from
 * the plan), and the submit plan with its fixed and derived values. A page
 * that only calls `flow.submit.run()` cannot write a field the plan does not
 * know — there is no way to spell it.
 *
 * YOU decide what a person notices, through the options:
 *   steps     which wizard step asks which field (default: one step per pick,
 *             then one for the typed fields, then "Prüfen" = step 3)
 *   items     how a search hit is displayed per pick (title, subtitle, status …)
 *   initial   prefills for typed fields
 *   messages  the sentence for an empty required field, per field
 *
 *   const flow = useAngebotsstatusAendernFlow({
 *     steps: { angebote: 1, angebotsstatus: 2 },
 *     items: { angebote: r => ({ id: r.id, title: fieldText(r, 'dauer') }) },
 *   });
 *   <IntentWizardShell forms={flow.forms} draftKey={flow.draftKey} …>
 *     // the record this flow changes: <EntitySelectStep {...flow.picks.angebote.select} {...flow.pick('angebote')} />
 *     <Bound form={flow.forms.angebote} name="angebotsstatus" />
 *     <StepNav onNext={() => flow.validateStep(n)} />
 *     {!flow.submit.done && <SummaryStep forms={flow.formList} submit={flow.submit} />}
 *     {flow.submit.result && <SuccessStep result={flow.submit.result} forms={flow.formList} submit={flow.submit} />}
 *   </IntentWizardShell>
 */
```
- `export type AngebotsstatusAendernFieldKey = 'angebote' | 'angebotsstatus';`
```ts
export interface AngebotsstatusAendernForms {
  angebote: StepForm<'angebote'>;
}
```
```ts
export interface AngebotsstatusAendernFlowOptions {
  /** field → wizard step that asks it; drives „Ändern“ links and answer chips. */
  steps?: Partial<Record<Key, number>>;
  initial?: Partial<Record<Key, unknown>>;
  messages?: Partial<Record<Key, string>>;
  /** How a search hit reads — the card's title/subtitle/status per pick. */
  items?: {
    angebote?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
  };
}
```
- `export const ANGEBOTSSTATUSAENDERN_REVIEW_STEP = 3;`
- `export function useAngebotsstatusAendernFlow(options: AngebotsstatusAendernFlowOptions = {})`
- `export type AngebotsstatusAendernFlow = ReturnType<typeof useAngebotsstatusAendernFlow>;`

### `lib/journey/flows/ProjektAnlegen`
```ts
/**
 * useProjektAnlegenFlow — the plumbing of the flow « Projekt anlegen », generated from the plan.
 *
 * Writes `projekte`: asks `kunde`, `projektart`, `projektstart_monat`, `projektstart_jahr`, `projektleitung`, `ansprechpartner_kunde`, `letzter_schritt`, `projektkennung`; sets `projektstatus` itself.
 * The hook OWNS: the form(s) with exactly these fields and the plan's required
 * ingredients, one record search per picked field (columns and filter from
 * the plan), and the submit plan with its fixed and derived values. A page
 * that only calls `flow.submit.run()` cannot write a field the plan does not
 * know — there is no way to spell it.
 *
 * YOU decide what a person notices, through the options:
 *   steps     which wizard step asks which field (default: one step per pick,
 *             then one for the typed fields, then "Prüfen" = step 4)
 *   items     how a search hit is displayed per pick (title, subtitle, status …)
 *   initial   prefills for typed fields
 *   messages  the sentence for an empty required field, per field
 *
 *   const flow = useProjektAnlegenFlow({
 *     steps: { kunde: 1, projektleitung: 2, projektart: 3, projektstart_monat: 3, projektstart_jahr: 3, ansprechpartner_kunde: 3, letzter_schritt: 3, projektkennung: 3 },
 *     items: { kunde: r => ({ id: r.id, title: fieldText(r, 'kundenname') }) },
 *   });
 *   <IntentWizardShell forms={flow.forms} draftKey={flow.draftKey} …>
 *     <EntitySelectStep {...flow.picks.kunde.select} {...flow.pick('kunde')} />
 *     <EntitySelectStep {...flow.picks.projektleitung.select} {...flow.pick('projektleitung')} />
 *     <Bound form={flow.forms.projekte} name="projektart" />
 *     <Bound form={flow.forms.projekte} name="projektstart_monat" />
 *     <Bound form={flow.forms.projekte} name="projektstart_jahr" />
 *     <Bound form={flow.forms.projekte} name="ansprechpartner_kunde" />
 *     <Bound form={flow.forms.projekte} name="letzter_schritt" />
 *     <Bound form={flow.forms.projekte} name="projektkennung" />
 *     <StepNav onNext={() => flow.validateStep(n)} />
 *     {!flow.submit.done && <SummaryStep forms={flow.formList} submit={flow.submit} />}
 *     {flow.submit.result && <SuccessStep result={flow.submit.result} forms={flow.formList} submit={flow.submit} />}
 *   </IntentWizardShell>
 */
```
- `export type ProjektAnlegenFieldKey = 'ansprechpartner_kunde' | 'kunde' | 'letzter_schritt' | 'projektart' | 'projektkennung' | 'projektleitung' | 'projektstart_jahr' | 'projektstart_monat';`
```ts
export interface ProjektAnlegenForms {
  projekte: StepForm<'projekte'>;
}
```
```ts
export interface ProjektAnlegenFlowOptions {
  /** field → wizard step that asks it; drives „Ändern“ links and answer chips. */
  steps?: Partial<Record<Key, number>>;
  initial?: Partial<Record<Key, unknown>>;
  messages?: Partial<Record<Key, string>>;
  /** How a search hit reads — the card's title/subtitle/status per pick. */
  items?: {
    kunde?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
    projektleitung?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
  };
}
```
- `export const PROJEKTANLEGEN_REVIEW_STEP = 4;`
- `export function useProjektAnlegenFlow(options: ProjektAnlegenFlowOptions = {})`
- `export type ProjektAnlegenFlow = ReturnType<typeof useProjektAnlegenFlow>;`

### `lib/journey/flows/ProjektstatusAendern`
```ts
/**
 * useProjektstatusAendernFlow — the plumbing of the flow « Projektstatus ändern », generated from the plan.
 *
 * Changes `projekte`: the record to change is picked (`flow.pick('projekte')`), the form is prefilled with its values; asks `projektstatus`, `letzter_schritt`.
 * The hook OWNS: the form(s) with exactly these fields and the plan's required
 * ingredients, one record search per picked field (columns and filter from
 * the plan), and the submit plan with its fixed and derived values. A page
 * that only calls `flow.submit.run()` cannot write a field the plan does not
 * know — there is no way to spell it.
 *
 * YOU decide what a person notices, through the options:
 *   steps     which wizard step asks which field (default: one step per pick,
 *             then one for the typed fields, then "Prüfen" = step 3)
 *   items     how a search hit is displayed per pick (title, subtitle, status …)
 *   initial   prefills for typed fields
 *   messages  the sentence for an empty required field, per field
 *
 *   const flow = useProjektstatusAendernFlow({
 *     steps: { projekte: 1, projektstatus: 2, letzter_schritt: 2 },
 *     items: { projekte: r => ({ id: r.id, title: fieldText(r, 'projektkennung') }) },
 *   });
 *   <IntentWizardShell forms={flow.forms} draftKey={flow.draftKey} …>
 *     // the record this flow changes: <EntitySelectStep {...flow.picks.projekte.select} {...flow.pick('projekte')} />
 *     <Bound form={flow.forms.projekte} name="projektstatus" />
 *     <Bound form={flow.forms.projekte} name="letzter_schritt" />
 *     <StepNav onNext={() => flow.validateStep(n)} />
 *     {!flow.submit.done && <SummaryStep forms={flow.formList} submit={flow.submit} />}
 *     {flow.submit.result && <SuccessStep result={flow.submit.result} forms={flow.formList} submit={flow.submit} />}
 *   </IntentWizardShell>
 */
```
- `export type ProjektstatusAendernFieldKey = 'letzter_schritt' | 'projekte' | 'projektstatus';`
```ts
export interface ProjektstatusAendernForms {
  projekte: StepForm<'projekte'>;
}
```
```ts
export interface ProjektstatusAendernFlowOptions {
  /** field → wizard step that asks it; drives „Ändern“ links and answer chips. */
  steps?: Partial<Record<Key, number>>;
  initial?: Partial<Record<Key, unknown>>;
  messages?: Partial<Record<Key, string>>;
  /** How a search hit reads — the card's title/subtitle/status per pick. */
  items?: {
    projekte?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
  };
}
```
- `export const PROJEKTSTATUSAENDERN_REVIEW_STEP = 3;`
- `export function useProjektstatusAendernFlow(options: ProjektstatusAendernFlowOptions = {})`
- `export type ProjektstatusAendernFlow = ReturnType<typeof useProjektstatusAendernFlow>;`

### `lib/journey/flows/RechnungErstellen`
```ts
/**
 * useRechnungErstellenFlow — the plumbing of the flow « Rechnung erstellen », generated from the plan.
 *
 * Writes `rechnungen`: asks `kunde`, `projekt`, `berater`, `zeiterfassungseintraege`, `rechnungsdatum`, `faelligkeitsdatum`, `rechnungsmonat`, `rechnungsjahr`, `nettobetrag`, `mehrwertsteuer`, `notizen`; sets `rechnungsstatus` itself.
 * The hook OWNS: the form(s) with exactly these fields and the plan's required
 * ingredients, one record search per picked field (columns and filter from
 * the plan), and the submit plan with its fixed and derived values. A page
 * that only calls `flow.submit.run()` cannot write a field the plan does not
 * know — there is no way to spell it.
 *
 * YOU decide what a person notices, through the options:
 *   steps     which wizard step asks which field (default: one step per pick,
 *             then one for the typed fields, then "Prüfen" = step 6)
 *   items     how a search hit is displayed per pick (title, subtitle, status …)
 *   initial   prefills for typed fields
 *   messages  the sentence for an empty required field, per field
 *
 *   const flow = useRechnungErstellenFlow({
 *     steps: { kunde: 1, projekt: 2, berater: 3, zeiterfassungseintraege: 4, rechnungsdatum: 5, faelligkeitsdatum: 5, rechnungsmonat: 5, rechnungsjahr: 5, nettobetrag: 5, mehrwertsteuer: 5, notizen: 5 },
 *     items: { kunde: r => ({ id: r.id, title: fieldText(r, 'kundenname') }) },
 *   });
 *   <IntentWizardShell forms={flow.forms} draftKey={flow.draftKey} …>
 *     <EntitySelectStep {...flow.picks.kunde.select} {...flow.pick('kunde')} />
 *     <EntitySelectStep {...flow.picks.projekt.select} {...flow.pick('projekt')} />
 *     <EntitySelectStep {...flow.picks.berater.select} {...flow.pickMany('berater')} />
 *     <EntitySelectStep {...flow.picks.zeiterfassungseintraege.select} {...flow.pickMany('zeiterfassungseintraege')} />
 *     <Bound form={flow.forms.rechnungen} name="rechnungsdatum" />
 *     <Bound form={flow.forms.rechnungen} name="faelligkeitsdatum" />
 *     <Bound form={flow.forms.rechnungen} name="rechnungsmonat" />
 *     <Bound form={flow.forms.rechnungen} name="rechnungsjahr" />
 *     <Bound form={flow.forms.rechnungen} name="nettobetrag" />
 *     <Bound form={flow.forms.rechnungen} name="mehrwertsteuer" />
 *     <Bound form={flow.forms.rechnungen} name="notizen" />
 *     <StepNav onNext={() => flow.validateStep(n)} />
 *     {!flow.submit.done && <SummaryStep forms={flow.formList} submit={flow.submit} />}
 *     {flow.submit.result && <SuccessStep result={flow.submit.result} forms={flow.formList} submit={flow.submit} />}
 *   </IntentWizardShell>
 */
```
- `export type RechnungErstellenFieldKey = 'berater' | 'faelligkeitsdatum' | 'kunde' | 'mehrwertsteuer' | 'nettobetrag' | 'notizen' | 'projekt' | 'rechnungsdatum' | 'rechnungsjahr' | 'rechnungsmonat' | 'zeiterfassungseintraege';`
```ts
export interface RechnungErstellenForms {
  rechnungen: StepForm<'rechnungen'>;
}
```
```ts
export interface RechnungErstellenFlowOptions {
  /** field → wizard step that asks it; drives „Ändern“ links and answer chips. */
  steps?: Partial<Record<Key, number>>;
  initial?: Partial<Record<Key, unknown>>;
  messages?: Partial<Record<Key, string>>;
  /** How a search hit reads — the card's title/subtitle/status per pick. */
  items?: {
    kunde?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
    projekt?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
    berater?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
    zeiterfassungseintraege?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
  };
}
```
- `export const RECHNUNGERSTELLEN_REVIEW_STEP = 6;`
- `export function useRechnungErstellenFlow(options: RechnungErstellenFlowOptions = {})`
- `export type RechnungErstellenFlow = ReturnType<typeof useRechnungErstellenFlow>;`

### `lib/journey/flows/StundenBuchen`
```ts
/**
 * useStundenBuchenFlow — the plumbing of the flow « Stunden buchen », generated from the plan.
 *
 * Writes `zeiterfassung`: asks `berater`, `projekt`, `leistung`, `datum`, `stunden`, `taetigkeit`, `erfassungsmonat`, `erfassungsjahr`, `abrechenbar`.
 * The hook OWNS: the form(s) with exactly these fields and the plan's required
 * ingredients, one record search per picked field (columns and filter from
 * the plan), and the submit plan with its fixed and derived values. A page
 * that only calls `flow.submit.run()` cannot write a field the plan does not
 * know — there is no way to spell it.
 *
 * YOU decide what a person notices, through the options:
 *   steps     which wizard step asks which field (default: one step per pick,
 *             then one for the typed fields, then "Prüfen" = step 5)
 *   items     how a search hit is displayed per pick (title, subtitle, status …)
 *   initial   prefills for typed fields
 *   messages  the sentence for an empty required field, per field
 *
 *   const flow = useStundenBuchenFlow({
 *     steps: { berater: 1, projekt: 2, leistung: 3, datum: 4, stunden: 4, taetigkeit: 4, erfassungsmonat: 4, erfassungsjahr: 4, abrechenbar: 4 },
 *     items: { berater: r => ({ id: r.id, title: fieldText(r, 'vorname') }) },
 *   });
 *   <IntentWizardShell forms={flow.forms} draftKey={flow.draftKey} …>
 *     <EntitySelectStep {...flow.picks.berater.select} {...flow.pick('berater')} />
 *     <EntitySelectStep {...flow.picks.projekt.select} {...flow.pick('projekt')} />
 *     <EntitySelectStep {...flow.picks.leistung.select} {...flow.pick('leistung')} />
 *     <Bound form={flow.forms.zeiterfassung} name="datum" />
 *     <Bound form={flow.forms.zeiterfassung} name="stunden" />
 *     <Bound form={flow.forms.zeiterfassung} name="taetigkeit" />
 *     <Bound form={flow.forms.zeiterfassung} name="erfassungsmonat" />
 *     <Bound form={flow.forms.zeiterfassung} name="erfassungsjahr" />
 *     <Bound form={flow.forms.zeiterfassung} name="abrechenbar" />
 *     <StepNav onNext={() => flow.validateStep(n)} />
 *     {!flow.submit.done && <SummaryStep forms={flow.formList} submit={flow.submit} />}
 *     {flow.submit.result && <SuccessStep result={flow.submit.result} forms={flow.formList} submit={flow.submit} />}
 *   </IntentWizardShell>
 */
```
- `export type StundenBuchenFieldKey = 'abrechenbar' | 'berater' | 'datum' | 'erfassungsjahr' | 'erfassungsmonat' | 'leistung' | 'projekt' | 'stunden' | 'taetigkeit';`
```ts
export interface StundenBuchenForms {
  zeiterfassung: StepForm<'zeiterfassung'>;
}
```
```ts
export interface StundenBuchenFlowOptions {
  /** field → wizard step that asks it; drives „Ändern“ links and answer chips. */
  steps?: Partial<Record<Key, number>>;
  initial?: Partial<Record<Key, unknown>>;
  messages?: Partial<Record<Key, string>>;
  /** How a search hit reads — the card's title/subtitle/status per pick. */
  items?: {
    berater?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
    projekt?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
    leistung?: (record: JourneyRecord, ctx: RefContext) => SelectItemLike;
  };
}
```
- `export const STUNDENBUCHEN_REVIEW_STEP = 5;`
- `export function useStundenBuchenFlow(options: StundenBuchenFlowOptions = {})`
- `export type StundenBuchenFlow = ReturnType<typeof useStundenBuchenFlow>;`

## @/lib/journey — exports by module

### policy
```ts
export interface FieldPolicyRule {
  hidden?: boolean;
  required?: boolean;
  label?: string;
  /** The owner's fixed value — the server presets it, the visitor never sees the field. */
  fixed?: unknown;
}
```
- `export type FieldPolicy = Record<string, Record<string, FieldPolicyRule>>;`
- `export function setFieldPolicy(fields: FieldPolicy | undefined | null): void`
- `export function fieldPolicy(entity: string, key: string): FieldPolicyRule`
- `export function isHiddenByPolicy(entity: string, key: string): boolean`
- `export function policyLabel(entity: string, key: string): string | undefined`
- `export function policyRequired(entity: string, key: string): boolean | undefined`
- `export function policyVersion(): number`
- `export function onPolicyChange(fn: () => void): () => void`

### usePolicy
- `export function usePolicyVersion(): number`

### port
```ts
export interface JourneyRecord {
  /** 24 lowercase hex chars — the platform record id. */
  id: string;
  fields: Record<string, unknown>;
  createdAt: string | null;
}
```
```ts
export interface ListOptions {
  limit?: number;
  offset?: number;
  /** vSQL order expressions, e.g. */
  orderby?: string[];
  /** Field projection. */
  fields?: string[];
  /** Server-side text search over string fields (internal) / client-side over the loaded page (public). */
  search?: { query: string; fields: string[] };
  /** A standing vSQL restriction, e.g. */
  filter?: string;
  signal?: AbortSignal;
}
```
```ts
export interface JourneyPort {
  readonly door: 'internal' | 'public';
  list(entity: EntityKey, opts?: ListOptions): Promise<JourneyRecord[]>;
  /** Record count (optionally filtered by the same search). */
  count(entity: EntityKey, opts?: { search?: { query: string; fields: string[] }; filter?: string; signal?: AbortSignal }): Promise<number | null>;
  /** ONE record by id (a linked record behind `fieldRef`, a record from the URL), or null. */
  get(entity: EntityKey, id: string): Promise<JourneyRecord | null>;
  /** Takes FORM values (record ids, lookup keys, ISO strings). */
  create(entity: EntityKey, values: Record<string, unknown>): Promise<JourneyRecord>;
  /** The applookup value for a record of app `appId`, in the form this door needs (REST URL internally, grant-scoped ref p… */
  ref(appId: string, recordId: string): string;
}
```
```ts
export interface InternalJourneyPort extends JourneyPort {
  update(entity: EntityKey, id: string, values: Record<string, unknown>): Promise<JourneyRecord>;
}
```
- `export function canUpdate(port: JourneyPort): port is InternalJourneyPort`
- `export class JourneyPortError extends Error`
- `export function toWirePayload(entity: EntityKey, values: Record<string, unknown>, port: JourneyPort): Record<string, unknown>`
- `export function planProvidedKeys(step: { values?: unknown; link?: Record<string, string> } | undefined): Set<string>`

### rules
- `export type EntityKey = 'kunden' | 'berater' | 'leistungskatalog' | 'projekte' | 'angebote' | 'zeiterfassung' | 'rechnungen';`
```ts
export interface StringFields {
  "kunden": "kundenname" | "email" | "strasse" | "hausnummer" | "plz" | "ort" | "rechnungsstrasse" | "rechnungshausnummer" | "rechnungsplz" | "rechnungsort" | "ansprechpartner_titel" | "ansprechpartner_vorname" | "ansprechpartner_nachname" | "ansprechpartner_email" | "letzter_kontakt_ansprechpartner" | "notizen";
  "berater": "vorname" | "nachname" | "titel" | "strasse" | "hausnummer" | "plz" | "ort" | "email_beruflich" | "email_privat" | "sonstiges";
  "leistungskatalog": "leistungsname" | "beschreibung";
  "projekte": "projektkennung" | "ansprechpartner_kunde" | "letzter_schritt";
  "angebote": "dauer" | "beschreibung";
  "zeiterfassung": "taetigkeit";
  "rechnungen": "rechnungsnummer" | "notizen";
}
```
- `export type StringFieldKey<E extends EntityKey> = E extends keyof StringFields ? StringFields[E] : never;`
```ts
export interface RecordFields {
  "kunden": never;
  "berater": "leistungen" | "zugewiesene_projekte";
  "leistungskatalog": "ausfuehrende_berater";
  "projekte": "kunde" | "projektleitung";
  "angebote": "projekt" | "berater";
  "zeiterfassung": "berater" | "projekt" | "leistung";
  "rechnungen": "kunde" | "projekt" | "berater" | "zeiterfassungseintraege";
}
```
- `export type RecordFieldKey<E extends EntityKey> = E extends keyof RecordFields ? RecordFields[E] : never;`
- `export type FieldKind …`
```ts
export interface FieldRule {
  key: string;
  fulltype: string;
  kind: FieldKind;
  /** From the app's base view. */
  required: boolean;
  /** Build-time label — `labelOf()` prefers the runtime i18n bundle. */
  label: string;
  /** Whether a journey may write it (`file` is upload-only, never via a journey). */
  writable: boolean;
  maxLength?: number;
  /** lookup / multilookup: the ONLY valid write values. */
  options?: string[];
  /** record / multirecord: the target app (always) and its entity key (when inside this appgroup). */
  targetAppId?: string;
  targetEntity?: EntityKey;
  format?: 'currency';
  /** HTML autocomplete token derived from the field name (given-name, email, tel, …). */
  autoComplete?: string;
}
```
```ts
export interface EntityInfo {
  key: EntityKey;
  appId: string;
  label: string;
  /** PascalCase plural — `get<pascal>()` on the service. */
  pascal: string;
  /** The single-record suffix — `create<single>()` on the service. */
  single: string;
}
```
- `export type Shape …`
- `export const ENTITIES: Record<EntityKey, EntityInfo> = { …`
- `export const FIELD_RULES: Record<EntityKey, Record<string, FieldRule>> = { …`
- `export const SHAPES: Record<EntityKey, Shape[]> = { …`
- `export const DISPLAY_FIELDS: Record<EntityKey, string[]> = { …`
- `export function displayNameOf(entity: EntityKey, fields: Record<string, unknown>): string`
- `export function ruleOf(entity: EntityKey, key: string): FieldRule | undefined`
- `export function labelOf(entity: EntityKey, key: string): string`
- `export function entityLabel(entity: EntityKey): string`
- `export function optionsOf(entity: EntityKey, key: string): Array<{ key: string; label: string }>`
- `export function isEmptyValue(v: unknown): boolean`

### messages
```ts
export interface MessageFields {
  "kunden": "kundenname" | "kundentyp" | "email" | "anlagedatum" | "strasse" | "hausnummer" | "plz" | "ort" | "rechnungsadresse_gleich" | "rechnungsstrasse" | "rechnungshausnummer" | "rechnungsplz" | "rechnungsort" | "ansprechpartner_titel" | "ansprechpartner_vorname" | "ansprechpartner_nachname" | "ansprechpartner_email" | "bevorzugte_kontaktart" | "letzter_kontakt_datum" | "letzter_kontakt_ansprechpartner" | "notizen";
  "berater": "vorname" | "nachname" | "titel" | "strasse" | "hausnummer" | "plz" | "ort" | "email_beruflich" | "email_privat" | "einstiegsdatum" | "status" | "stundensatz" | "stunden_aktueller_monat" | "stunden_aktuelles_quartal" | "stunden_aktuelles_jahr" | "stunden_letzter_monat" | "stunden_letztes_quartal" | "stunden_letztes_jahr" | "sonstiges" | "leistungen" | "zugewiesene_projekte";
  "leistungskatalog": "leistungsname" | "leistungstyp" | "beschreibung" | "kostenvoranschlag" | "einheit" | "ausfuehrende_berater";
  "projekte": "projektkennung" | "projektnummer" | "projektart" | "projektstatus" | "projektstart_monat" | "projektstart_jahr" | "kunde" | "ansprechpartner_kunde" | "letzter_schritt" | "projektleitung";
  "angebote": "angebotsnummer" | "angebotsjahr" | "angebotstyp" | "angebotsstatus" | "zeitrahmen_anfang" | "zeitrahmen_ende" | "dauer" | "kostentyp" | "kostenbetrag" | "beschreibung" | "projekt" | "berater";
  "zeiterfassung": "berater" | "projekt" | "leistung" | "datum" | "stunden" | "erfassungsmonat" | "erfassungsjahr" | "abrechenbar" | "taetigkeit";
  "rechnungen": "rechnungsnummer" | "rechnungsdatum" | "faelligkeitsdatum" | "rechnungsstatus" | "rechnungsmonat" | "rechnungsjahr" | "kunde" | "nettobetrag" | "mehrwertsteuer" | "gesamtbetrag" | "notizen" | "projekt" | "berater" | "zeiterfassungseintraege";
}
```
- `export type MessageFieldKey<E extends EntityKey> = E extends keyof MessageFields ? MessageFields[E] : never;`
- `export const REQUIRED_MESSAGES: { [E in EntityKey]?: Partial<Record<MessageFieldKey<E>, string>> } = { …`
- `export function requiredMessage(entity: EntityKey, key: string): string`
- `export function hasOwnMessage(entity: EntityKey, key: string): boolean`

### format
- `export const EMPTY_VALUE = '—';`
- `export function formatFieldValue(entity: EntityKey, key: string, value: unknown, labels: Record<string, string> = {}): string`
- `export type RangeUnit = 'nights' | 'days' | 'none';`
- `export function formatRange(fromIso: string | null | undefined, toIso: string | null | undefined, unit: RangeUnit = 'nights'): string`
- `export function todayIso(): string`
- `export function nowIso(): string`

### reference
- `export function makeReference(entity: EntityKey | string, recordId: string, prefix?: string): string`

### draft
- `export const DRAFT_CLEARED_EVENT = 'journey-draft-cleared';`
```ts
export interface JourneyDraft {
  step: number;
  /** One entry per form, in the order the shell received them. */
  data: Array<Record<string, unknown>>;
  labels?: Array<Record<string, string>>;
  savedAt: number;
}
```
- `export const DRAFT_MAX_AGE_MS = 30 * 86_400_000;`
- `export function draftStorageKey(draftKey: string): string`
- `export function draftHasValues(draft: Pick<JourneyDraft, 'data'>): boolean`
- `export function removeJourneyDraft(draftKey: string): void`
- `export function readJourneyDraft(draftKey: string): JourneyDraft | null`
- `export function writeJourneyDraft(draftKey: string, draft: JourneyDraft): void`
- `export function clearJourneyDraft(draftKey: string): void`

### occupancy
```ts
export interface OccupancyRange {
  start: string;
  end?: string | null;
}
```
```ts
export interface OccupancyOptions {
  /** The picked resource's record id (room, vehicle) — when the rule names a resource. */
  resource?: string | null;
  /** Count cancelled/free records as occupied too (default false). */
  includeFree?: boolean;
}
```
- `export function occupancyRuleOf(entity: EntityKey | string): OccupancyRule | undefined`
- `export function occupancyFor(entity: EntityKey | string, records: readonly RecordLike[], opts: OccupancyOptions = {}): OccupancyRange[]`

### selectMode
- `export type SelectMode = 'auto' | 'pills' | 'cards' | 'combobox';`
- `export type ResolvedSelectMode = Exclude<SelectMode, 'auto'>;`
- `export const COMPACT_MAX = 5;`
- `export const CARDS_MAX = 50;`
- `export const SERVER_SEARCH_FROM = 200;`
- `export const SEARCH_PAGE_SIZE = 50;`
- `export const SEARCH_MIN_CHARS = 2;`
- `export const SEARCH_DEBOUNCE_MS = 300;`
- `export const SEARCH_QUERY_MAX = 60;`
- `export function resolveSelectMode(total: number, requested: SelectMode = 'auto'): ResolvedSelectMode`

### search
- `export function sanitizeSearchQuery(raw: string): string`
- `export function buildSearchFilter(query: string, fields: string[]): string | null`
- `export function byIdFilter(id: string): string`
- `export function refFilter(field: string, id: string): string`
- `export function combineFilters(...parts: Array<string | null | undefined>): string | undefined`
- `export function matchesSearch(fields: Record<string, unknown>, query: string, searchFields: string[]): boolean`

### fields
```ts
export interface LookupValue {
  key: string;
  label: string;
}
```
- `export function fieldLookup(r: JourneyRecord, key: string): LookupValue | null`
- `export function fieldLookups(r: JourneyRecord, key: string): LookupValue[]`
- `export function fieldText(r: JourneyRecord, key: string): string`
- `export function fieldNumber(r: JourneyRecord, key: string): number | null`
- `export function fieldDate(r: JourneyRecord, key: string): string | null`
- `export function fieldRef(r: JourneyRecord, key: string): string | null`

### recordLabels
- `export function recordIdOf(value: unknown): string | null`
- `export function rememberRecordLabel(id: string, label: string | undefined): void`
- `export function recordLabelOf(value: unknown): string | undefined`

### flowLinks
- `export function flowTokens(pathOrSlug: string): string[]`
- `export function resolveFlowHref(href: string, paths: readonly string[]): string | null`

### useRecordSearch
```ts
export interface SelectItemLike { id: string; title: string; }
```
```ts
export interface RefContext {
  /** Display name(s) of the record(s) an applookup/multipleapplookup field points at (`displayNameOf` of the target entity… */
  ref(key: string): string | undefined;
}
```
```ts
export interface RecordSearchOptions<T extends SelectItemLike, E extends EntityKey = EntityKey> {
  /** The entity's text fields the search runs over — TYPED from the generated rules: a field of another entity or a non-te… */
  searchFields: StringFieldKey<E>[];
  /** The card for a record. */
  toItem?: (record: JourneyRecord, ctx: RefContext) => T;
  /** vSQL order, e.g. */
  orderby?: string[];
  /** The step's standing restriction as vSQL — "r.v_status == 'verfuegbar'", "r.v_rueckgabedatum is None". */
  filter?: string;
  /** A client-side restriction over the records the door handed out — the door-agnostic fallback for what vSQL cannot say,… */
  where?: (record: JourneyRecord) => boolean;
  /** Below this count everything is loaded once and searched client-side (default SERVER_SEARCH_FROM). */
  loadAllUpTo?: number;
  pageSize?: number;
}
```
```ts
export interface RecordSearch<T extends SelectItemLike> {
  /** Spread into <EntitySelectStep {...search.select} onSelect={…} />. */
  select: {
    items: T[];
    totalCount: number | null;
    onSearch?: (query: string, signal: AbortSignal) => Promise<T[]>;
    loading: boolean;
    error: string | null;
    entity: EntityKey;
    port: JourneyPort;
    filtered: boolean;
    adopt: (record: JourneyRecord) => void;
  };
  /** The loaded RECORDS behind `select.items`, same order and restriction — for a rule over the list (a count, a sum, "is… */
  records: JourneyRecord[];
  /** Display name of any record seen so far (first page or a search hit) — for f.set(key, id, label). */
  labelOf(id: string): string | undefined;
  /** `ctx.ref(key)` for a record outside `toItem` (a fact line, a summary item). */
  refLabel(record: JourneyRecord, key: string): string | undefined;
  /** The record behind an id the user could pick — already loaded, no request. */
  recordOf(id: string): JourneyRecord | undefined;
  /** Take a record the page just created into the list and the label map — no request, the record is known. */
  adopt(record: JourneyRecord): void;
  /** Re-run the initial load (after a write elsewhere). */
  reload(): Promise<void>;
}
```
- `export function decideStrategy(count: number | null, loadAllUpTo: number = SERVER_SEARCH_FROM, pageSize: number = SEARCH_PAGE_SIZE): { serverSearch: boolean; limit?: number }`
- `export function useRecordSearch<E extends EntityKey, T extends SelectItemLike>(port: JourneyPort, entity: E, options: RecordSearchOptions<T, E>): RecordSearch<T>`

### useOccupancy
```ts
export interface Occupancy {
  /** The nights taken — for `opts.resource` when given, else across every resource. */
  blocked: OccupancyRange[];
  /** Is the stay [from, to) free for this resource? Departure day exclusive; an incomplete pair is not free. */
  isFree(from: string | null | undefined, to: string | null | undefined, resource?: string | null): boolean;
  /** `where` for the resource's pick step: only resources free in the picked stay (no stay yet → all). */
  freeIn(from: string | null | undefined, to: string | null | undefined): (resource: JourneyRecord) => boolean;
  /** Whether the entity has an occupancy rule at all. */
  ruled: boolean;
  loading: boolean;
  error: string | null;
  reload(): Promise<void>;
}
```
- `export function useOccupancy(port: JourneyPort, entity: EntityKey, opts: { resource?: string | null } = {}): Occupancy`

### useRecordCount
```ts
export interface RecordCount {
  /** null while loading, while `enabled` is false, or after an error. */
  count: number | null;
  loading: boolean;
  error: string | null;
  reload(): Promise<void>;
}
```
```ts
export interface RecordCountOptions {
  /** vSQL restriction (internal door). */
  filter?: string;
  /** Its TypeScript twin — the only restriction the public door can apply. */
  where?: (record: JourneyRecord) => boolean;
  /** false = the condition is not complete yet; nothing is requested. */
  enabled?: boolean;
}
```
- `export function useRecordCount(port: JourneyPort, entity: EntityKey, opts: RecordCountOptions = {}): RecordCount`

### useStepForm
- `export type FormValues = Record<string, unknown>;`
- `export function setActiveWizardStep(step: number | undefined): void`
```ts
export interface SummaryItem {
  key: string;
  label: string;
  value: string;
  /** Wizard step that edits this value — enables "Change" in the summary. */
  step?: number;
  /** All field keys behind this row (a range row has two). */
  keys?: string[];
  /** The element to focus when the user wants to change it. */
  fieldId?: string;
}
```
```ts
export interface FormError {
  key: string;
  label: string;
  message: string;
  fieldId: string;
}
```
```ts
export interface StepFormOptions {
  /** Subset and order of fields (default: every writable field of the entity). */
  fields?: string[];
  /** field key → wizard step number. */
  steps?: Record<string, number>;
  /** Override the platform's `required` per field (public pages declare their own). */
  required?: Record<string, boolean>;
  /** What to say when a required field is empty, per field — when THIS flow knows more than the entity („Bitte den Einsatz… */
  messages?: Record<string, string>;
  initial?: FormValues;
  /** Prefix for element ids (default: the entity key). */
  id?: string;
  /** Browser autofill of the person's OWN data (given-name, email, postal-code, … from the generated rules). */
  autoComplete?: boolean;
}
```
```ts
export interface RangeOptions {
  blocked?: Array<{ start: string; end?: string | null }>;
  minNights?: number;
  /** How the pair counts in messages and the summary: 'nights' (a stay), 'days' (a course, a loan, a period) or 'none'. */
  unit?: RangeUnit;
}
```
```ts
export interface FieldProps {
  id: string;
  name: string;
  value: string;
  onChange: (next: ChangeLike) => void;
  onBlur: () => void;
  required: boolean;
  type?: string;
  inputMode?: 'text' | 'decimal' | 'numeric' | 'tel' | 'email' | 'url';
  autoComplete?: string;
  maxLength?: number;
  step?: string;
  'aria-required'?: true;
  'aria-invalid'?: true;
  'aria-describedby'?: string;
}
```
```ts
export interface DateProps {
  id: string;
  value: string | null;
  onChange: (next: string | null) => void;
  required: boolean;
  invalid: boolean;
  mode: 'date' | 'datetime';
}
```
```ts
export interface ChoiceProps {
  /** Points at the <Field> label — a radiogroup is not labelable by htmlFor. */
  'aria-labelledby'?: string;
  id: string;
  value: string | null;
  onChange: (key: string | null) => void;
  options: Array<{ key: string; label: string }>;
  required: boolean;
  invalid: boolean;
  'aria-describedby'?: string;
}
```
```ts
export interface CheckboxProps {
  id: string;
  checked: boolean;
  onCheckedChange: (checked: boolean | 'indeterminate') => void;
}
```
```ts
export interface RecordProps {
  id: string;
  value: string | null;
  onChange: (id: string | null) => void;
  invalid: boolean;
}
```
```ts
export interface RecordsProps {
  id: string;
  selectedIds: string[];
  onToggle: (id: string) => void;
  invalid: boolean;
  'aria-describedby'?: string;
}
```
```ts
export interface RangeProps {
  value: { from: string | null; to: string | null };
  onChange: (range: { from: string | null; to: string | null }) => void;
  blocked: Array<{ start: string; end?: string | null }>;
  minNights: number;
}
```
- `export type SetArgs<E extends EntityKey, K extends string> = K extends RecordFieldKey<E> …`
```ts
export interface StepForm<E extends EntityKey = EntityKey> {
  readonly entity: E;
  readonly id: string;
  readonly keys: string[];
  /** Keys of `fields` the owner's policy hides — the shell skips a step that has only these. */
  readonly hiddenKeys: string[];
  readonly rules: Record<string, FieldRule>;
  readonly values: FormValues;
  /** Display names learned while picking records, keyed by field key and by record id. */
  readonly labels: Record<string, string>;
  get(key: string): unknown;
  set<K extends string>(key: K, ...rest: SetArgs<E, K>): void;
  /** Teach the form a record's display name (for summaries of multi-record fields). */
  remember(recordId: string, label: string): void;
  reset(values?: FormValues, labels?: Record<string, string>): void;
  field(key: string): FieldProps;
  number(key: string): FieldProps;
  date(key: string): DateProps;
  choice(key: string): ChoiceProps;
  checkbox(key: string): CheckboxProps;
  record(key: string): RecordProps;
  /** Binding for a multipleapplookup: the value is a `string[]` of record ids, required means "at least one". */
  records(key: string, labelOf?: (id: string) => string | undefined): RecordsProps;
  range(fromKey: string, toKey: string, opts?: RangeOptions): RangeProps;
  /** The visible error of a field (after touch or a validate() attempt). */
  error(key: string): string | undefined;
  /** True once the user touched the field and it is filled and valid. */
  ok(key: string): boolean;
  /** Errors of attempted fields — what a FieldErrorSummary lists. */
  readonly errors: FormError[];
  /** Labels of required fields that are still empty — regardless of touch state. */
  readonly missing: string[];
  readonly required: (key: string) => boolean;
  /** Validates the given fields (default all), marks them attempted, focuses the first invalid one. */
  validate(keys?: string[]): boolean;
  stepOf(key: string): number | undefined;
  /** True when the form ASKS for the field — listed in `fields` or given a step. */
  bound(key: string): boolean;
  fieldId(key: string): string;
  /** Formatted rows for a summary — a range shows as ONE row. */
  summary(): SummaryItem[];
  /** Values of the rule fields, empty ones removed — what `port.create` takes. */
  payload(): FormValues;
}
```
- `export function useStepForm<E extends EntityKey>(entity: E, options: StepFormOptions = {}): StepForm<E>`

### useJourneySubmit
```ts
export interface PlanContext {
  port: JourneyPort;
  /** Records of the steps that already succeeded, by step key. */
  done: Record<string, JourneyRecord>;
}
```
```ts
export interface PlanStep {
  key: string;
  /** Shown in the per-step status list (default: the entity label / the key). */
  label?: string;
  entity?: EntityKey;
  /** Its payload() is the base of the create. */
  form?: StepForm;
  /** Extra values merged over the form payload (static or computed from done steps). */
  values?: FormValues | ((ctx: PlanContext) => FormValues);
  /** field key → step key: the field receives the id of that done step's record. */
  link?: Record<string, string>;
  /** Steps that must be done first (the plan runs in order anyway). */
  needs?: string[];
  /** The id of the record this step CHANGES instead of creating one — a status flip, a return, a check-in. */
  updates?: string | ((ctx: PlanContext) => string | undefined);
  /** The record this journey is about — reference and facts come from it (default: first step). */
  primary?: boolean;
  /** What the step does to its record — drives the words in the plan list and the success heading ("angelegt" / "aktualisi… */
  verb?: 'create' | 'update';
  /** Escape hatch for anything that is not a plain create. */
  /** Return the record you touched (a MutationResult from update…Entry works) so the success screen gets its reference; re… */
  run?: (ctx: PlanContext) => Promise<JourneyRecord | { id?: string; record_id?: string; fields?: Record<string, unknown> } | void>;
}
```
- `export type PlanStepStatus = 'idle' | 'running' | 'done' | 'failed';`
```ts
export interface JourneyResult {
  primary: JourneyRecord;
  entity?: EntityKey;
  records: Record<string, JourneyRecord>;
  /** False when the primary step only updated an existing record — the success screen then says "aktualisiert", not "angel… */
  created: boolean;
  /** The review rows as confirmed (SummaryStep → remember) — the success screen's facts when no form carries them (update-… */
  summary?: SummaryItem[];
}
```
- `export function stepVerb(step: PlanStep): 'create' | 'update'`
```ts
export interface JourneySubmit {
  /** Runs the plan (skipping done steps). */
  run(): Promise<JourneyResult | null>;
  retry(): Promise<JourneyResult | null>;
  /** Forgets everything — for "create another one". */
  reset(): void;
  /** The review's rows, kept for the result (SummaryStep calls this on confirm). */
  remember(rows: SummaryItem[]): void;
  readonly submitting: boolean;
  readonly done: boolean;
  readonly error: Error | null;
  readonly result: JourneyResult | null;
  readonly status: Record<string, PlanStepStatus>;
  readonly failedStep: string | null;
  readonly doneCount: number;
  readonly plan: PlanStep[];
}
```
```ts
export interface JourneySubmitOptions {
  /** Cleared once the whole plan succeeded. */
  draftKey?: string;
  onSuccess?: (result: JourneyResult) => void;
}
```
- `export function useJourneySubmit(port: JourneyPort, plan: PlanStep[], options: JourneySubmitOptions = {}): JourneySubmit`

### summary
- `export function mergeSummaryRows(formRows: SummaryItem[], items: SummaryItem[]): SummaryItem[]`

### servicePort (`@/services/journeyPort`)
- `export const servicePort: InternalJourneyPort = { …`

### publicPort (`@/lib/journey/publicPort`)
- `export function hydrateLookups(entity: EntityKey, fields: Record<string, unknown>): Record<string, unknown>`
- `export function createPublicPort(cfg: PublicPagesConfig, page: PublicPageConfig): JourneyPort`

## Blocks — `@/components/blocks/<Name>` (exports + props contract)

### AvailabilityRangePicker — exports `isNightBlocked`, `rangeIsFree`, `AvailabilityRangePicker`
```ts
export interface AvailabilityRange {
  /** Arrival date, ISO `yyyy-MM-dd`. */
  start: string;
  /** Departure date, EXCLUSIVE. */
  end?: string | null;
}
```
```ts
export interface DateRangeValue {
  from: string | null;
  to: string | null;
}
```
- `export function isNightBlocked(dayIso: string, blocked: AvailabilityRange[]): boolean`
- `export function rangeIsFree(fromIso: string, toIso: string, blocked: AvailabilityRange[]): boolean`
```ts
interface AvailabilityRangePickerProps {
  blocked: AvailabilityRange[];
  value: DateRangeValue;
  onChange: (range: DateRangeValue) => void;
  /** Shortest allowed stay in nights (default 1). */
  minNights?: number;
  /** Months rendered side by side on wide containers (default 2). */
  months?: number;
  /** Days before today are inert (default true). */
  disablePast?: boolean;
  /** Colour key under the grid (default true). */
  legend?: boolean | string;
  /** What the pair means. */
  unit?: 'nights' | 'days';
  /** Any of the picker's own words, when the unit defaults do not fit. */
  texts?: Partial<{ pickStart: string; pickEnd: string; minHint: string }>;
}
```

### Bound — exports `controlFor`, `Bound`
- `export type BoundControl = 'input' | 'textarea' | 'date' | 'choice' | 'checkbox';`
```ts
export interface BoundProps {
  form: StepForm;
  name: string;
  /** Overrides the entity's field label. */
  label?: ReactNode;
  /** One short line under the control. */
  hint?: ReactNode;
  placeholder?: string;
  /** Textarea rows (default 3). */
  rows?: number;
  /** ChoiceGroup: clicking the selected pill again clears it. */
  allowClear?: boolean;
  /** Overrules the control the field's kind implies. */
  as?: BoundControl;
  className?: string;
}
```
- `export function controlFor(kind: FieldKind | undefined): BoundControl`

### BudgetTracker — exports `BudgetTracker`
- `export type BudgetFormat = 'currency' | 'count';`
```ts
export interface BudgetTrackerTexts {
  /** "Gebucht" / "Belegt" */
  booked: string;
  /** "von" */
  of: string;
  /** "Verbleibend" / "frei" */
  remaining: string;
  /** "Budget überschritten!" / "Kapazität überschritten" */
  over: string;
  /** Shown when budget <= 0: "Kein Budget definiert" / "Keine Kapazität festgelegt" */
  none: string;
}
```
```ts
export interface BudgetTrackerProps {
  budget: number;
  booked: number;
  label?: string;
  showRemaining?: boolean;
  /** What the numbers are: money (default, formatted as currency) or a plain count with `unit`. */
  format?: BudgetFormat;
  /** Count mode: the word after the number — "Plätze", "Stunden", "Stück". */
  unit?: string;
  /** Any word of the meter, when the defaults of the format do not fit. */
  texts?: Partial<BudgetTrackerTexts>;
}
```

### ChoiceGroup — exports `ChoiceGroup`
```ts
export interface ChoiceOption {
  key: string;
  label: string;
}
```
```ts
export interface ChoiceGroupProps {
  id: string;
  value: string | null;
  onChange: (key: string | null) => void;
  options: ChoiceOption[];
  required?: boolean;
  invalid?: boolean;
  /** Clicking the selected pill again clears it (default: false — a required choice cannot be unset). */
  allowClear?: boolean;
  'aria-describedby'?: string;
  'aria-labelledby'?: string;
  className?: string;
}
```

### EntitySelectStep — exports `EntitySelectStep`
```ts
export interface SelectItem {
  id: string;
  title: string;
  subtitle?: string;
  status?: { key: string; label: string };
  stats?: { label: string; value: string | number }[];
  icon?: ReactNode;
}
```
```ts
interface SinglePickProps {
  onSelect: (id: string) => void;
  /** Highlights the current pick (e.g. */
  selectedId?: string | null;
  selectedIds?: never;
  onToggle?: never;
}
```
```ts
interface MultiPickProps {
  /** Toggles one id in/out of the pick — from `f.records(key)`. */
  onToggle: (id: string) => void;
  selectedIds: string[];
  selectedId?: never;
  onSelect?: never;
}
```
- `export type PickProps = SinglePickProps | MultiPickProps;`
```ts
export interface EntitySelectStepBaseProps {
  items: SelectItem[];
  /** Element id of the step (from `f.records(key).id`) — what `validate()` focuses. */
  id?: string;
  /** Marks the step invalid (required pick missing) — from `f.records(key)`. */
  invalid?: boolean;
  'aria-describedby'?: string;
  /** The tile before the title when an item has no `icon`: two-letter initials (people) or nothing (rooms, tools, dates —… */
  avatar?: 'initials' | 'none';
  searchPlaceholder?: string;
  emptyIcon?: ReactNode;
  /** Overrides the layer's explanation of an empty list (e.g. */
  emptyText?: string;
  /** Label for the "create new" button (default "Neu erstellen"). */
  createLabel?: string;
  /** The mechanical "Neu anlegen": ON whenever `entity`/`port`/`adopt` come from `{...x.select}` on the internal door and… */
  create?: boolean | InlineCreateOptions;
  /** Hand-written alternative: reveal your own panel on click. */
  onCreateNew?: () => void;
  /** Your own mini-form panel, rendered above the list. */
  createDialog?: ReactNode;
  /** From `{...x.select}` (useRecordSearch) — what the mechanical create and the empty-state explanation need. */
  entity?: EntityKey;
  port?: JourneyPort;
  filtered?: boolean;
  adopt?: (record: JourneyRecord) => void;
  /** Card columns on wide screens. */
  columns?: 1 | 2;
  /** Form of the step. */
  mode?: SelectMode;
  /** Records the step could offer in total (server count). */
  totalCount?: number | null;
  /** Server-side search. */
  onSearch?: (query: string, signal: AbortSignal) => Promise<SelectItem[]>;
  /** Initial load pending (from useRecordSearch). */
  loading?: boolean;
  /** Load/search error to show (from useRecordSearch). */
  error?: string | null;
}
```
- `export type EntitySelectStepProps = EntitySelectStepBaseProps & PickProps;`
```ts
export interface InlineCreateOptions {
  /** Fields of the mini-form. */
  fields?: string[];
  initial?: FormValues;
  /** Panel heading. */
  title?: string;
}
```

### Field — exports `useEnclosingField`, `Field`
```ts
export interface FieldProps {
  form: StepForm;
  name: string;
  /** Overrides the entity's field label. */
  label?: ReactNode;
  /** One short line under the control (what to enter, an example). */
  hint?: ReactNode;
  /** The control carries its own visible text (a Checkbox with a sentence): keep the label for screen readers only. */
  hideLabel?: boolean;
  children: ReactNode;
  className?: string;
}
```
- `export function useEnclosingField(): string | null`

### FieldErrorSummary — exports `FieldErrorSummary`
```ts
export interface FieldErrorSummaryProps {
  forms: StepForm[];
  /** Only errors of fields that belong to this wizard step (fields without a step always show). */
  step?: number;
  className?: string;
}
```

### InlineCreate — exports `inlineCreateFields`, `canInlineCreate`, `InlineCreate`
- `export function inlineCreateFields(entity: EntityKey): string[]`
- `export function canInlineCreate(entity: EntityKey, fields: string[] = inlineCreateFields(entity)): boolean`
```ts
export interface InlineCreateProps {
  entity: EntityKey;
  /** The page's door — the same port the pick step's useRecordSearch uses. */
  port: JourneyPort;
  /** Fields of the mini-form. */
  fields?: string[];
  initial?: FormValues;
  /** Panel heading. */
  title?: string;
  onCreated: (record: JourneyRecord) => void;
  onCancel: () => void;
}
```

### IntentWizardShell — exports `WizardStep`, `useWizard`, `IntentWizardShell`
```ts
export interface WizardStep {
  label: string;
  /** Stable id for a step (optional; used in aria labels and tests). */
  key?: string;
  /** `false` hides the step (branching). */
  enabledIf?: boolean;
  /** Visible heading of the step's content (default: `label`). */
  heading?: string;
  /** One sentence under the heading: what to do here, in the user's words ("An- und Abreise wählen — belegte Nächte sind a… */
  description?: ReactNode;
  /** Field keys (of the shell's `forms`) this step needs filled. */
  needs?: string[];
}
```
```ts
export interface WizardStepProps extends WizardStep {
  children?: ReactNode;
}
```
```ts
export interface WizardIntro {
  /** One sentence: what this flow achieves. */
  description?: ReactNode;
  /** "Das brauchst du:" — what the user should have at hand. */
  needs?: string[];
  startLabel?: string;
  /** Rough duration shown as "ca. */
  estimatedMinutes?: number;
  /** How often the start screen opens on its own (default 1). */
  autoShow?: number;
}
```
```ts
export interface WizardContextValue {
  /** Current step as the PAGE numbers it (1-based index into `steps`). */
  step: number;
  /** Position among ENABLED steps (what the user sees: "Schritt 2 von 4"). */
  position: number;
  total: number;
  steps: WizardStep[];
  enabledSteps: number[];
  nextLabel?: string;
  /** Set by "Change" in the summary: Continue jumps back there. */
  returnTo: number | null;
  goTo(step: number, opts?: { focus?: string; returnTo?: number | null }): void;
  next(): void;
  prev(): void;
  /** The journey succeeded: drop the draft and stop saving until a restart. */
  markCompleted(): void;
  /** True after markCompleted(): the indicator shows every step as done and nothing navigates back into the finished journ… */
  completed: boolean;
  /** Hide the answer chips while the caller is mounted — SummaryStep's rows ARE the answers, chips above them doubled ever… */
  suppressChips(): () => void;
  /** Hide the shell's step heading while the caller is mounted — for blocks that bring their own (SummaryStep "Alles richt… */
  suppressHeading(): () => void;
}
```
- `export function useWizard(): WizardContextValue | null`
```ts
interface IntentWizardShellProps {
  /** Omit inside a PublicShell that already carries the title — otherwise the page shows the same heading twice (both rend… */
  title?: string;
  subtitle?: string;
  /** The step list — or leave it out and pass <WizardStep> children instead. */
  steps?: WizardStep[];
  currentStep: number;
  onStepChange: (step: number) => void;
  loading?: boolean;
  error?: Error | null;
  onRetry?: () => void;
  /** Header back link. */
  back?: { href: string; label: string } | false;
  /** The step forms — enables the draft, the answer chips and the error summary. */
  forms?: StepForm[];
  /** Persist the forms + step in localStorage under this key (namespaced per dashboard by the shell). */
  draftKey?: string;
  /** Optional start card on step 1: what this flow does, its steps, what to have at hand. */
  intro?: WizardIntro;
  /** The step content's surface. */
  surface?: 'card' | 'plain';
  children: ReactNode;
}
```

### StatusBadge — exports `StatusBadge`, `getStatusColor`
- `export type StatusTone = 'positive' | 'warning' | 'danger' | 'info' | 'neutral';`
```ts
interface StatusBadgeProps {
  statusKey: string | undefined;
  label?: string;
  /** Overrides the word table: `tone="neutral"` for a status whose colour the table guesses wrong. */
  tone?: StatusTone;
  className?: string;
}
```
- `export function getStatusColor(statusKey: string | undefined, tone?: StatusTone): string`

### StepNav — exports `StepNav`
```ts
export interface StepNavProps {
  onBack?: () => void;
  /** false → stay (generic hint); a string → stay and show it as the reason; true/void → continue. */
  onNext?: () => boolean | string | void | Promise<boolean | string | void>;
  /** Full label override. */
  nextLabel?: string;
  /** The next step's name → "Continue: <name>". */
  nextStepLabel?: string;
  backLabel?: string;
  nextDisabled?: boolean;
  hideBack?: boolean;
  busy?: boolean;
  /** Rendered between the two buttons (a hint, a counter, a secondary action). */
  children?: ReactNode;
  className?: string;
}
```

### SuccessStep — exports `SuccessStep`
```ts
export interface SuccessAction {
  label: string;
  /** `#/…` or `#/intents/<slug>` — never a platform record list. */
  href?: string;
  onClick?: () => void;
  icon?: ReactNode;
}
```
```ts
export interface SuccessStepProps {
  result: JourneyResult;
  /** Folgeaktionen — the first is rendered as the primary button. */
  next?: SuccessAction[];
  /** Default: "<Entity> angelegt". */
  title?: string;
  /** Facts are derived from these forms (filled fields only) … */
  forms?: StepForm[];
  /** … or passed explicitly. */
  facts?: Array<Pick<SummaryItem, 'label' | 'value'>>;
  whatHappensNext?: ReactNode;
  /** Reserved: prefix for a shown reference. */
  referencePrefix?: string;
  /** The heading's verb. */
  verb?: 'created' | 'updated';
  /** With the runner given, the first action is "Noch einmal": resets the plan, the forms and jumps to step 1 — the five-l… */
  submit?: JourneySubmit;
  restartLabel?: string;
  /** Copy and print are offered by default — an internal status flip may want neither. */
  actions?: { copy?: boolean; print?: boolean };
  printLabel?: string;
  children?: ReactNode;
}
```

### SummaryStep — exports `SummaryStep`
```ts
export interface SummaryStepProps {
  forms: StepForm[];
  submit: JourneySubmit;
  title?: string;
  /** One or two sentences: what happens after Confirm. */
  whatHappensNext?: ReactNode;
  confirmLabel?: string;
  /** Extra rows the forms do not know about (a computed total, a picked option). */
  items?: SummaryItem[];
  /** Override the jump: default uses the shell (goTo step + focus field + return here). */
  onEdit?: (step: number, fieldId?: string) => void;
  /** Rendered between the answers and the confirm area. */
  children?: ReactNode;
}
```
